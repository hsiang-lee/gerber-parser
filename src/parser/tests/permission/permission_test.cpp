#include <gtest/gtest.h>
#include "permission/permission.h"

/*TEST(PermissionTest, TestDefaultFileAccepted)
{
    Permission per;
    EXPECT_TRUE(per.accept());
}

TEST(PermissionTest, TestNotAccepted)
{
    Permission per(std::string(LicenseData) + "license1.txt");
    EXPECT_FALSE(per.accept());
}

TEST(PermissionTest, TestAccepted)
{
    Permission per(std::string(LicenseData) + "license.txt");
    EXPECT_TRUE(per.accept());
}

TEST(PermissionTest, TestNotExistLicense)
{
    Permission per(std::string(LicenseData) + "notexist.txt");
    EXPECT_FALSE(per.accept());
}*/
